@model Purchase
@{
    Layout = "~/Views/Shared/_InvoicePrintLayout.cshtml";
    ViewData["PrintPageTitle"] = $"فاتورة شراء #{Model.Id}";
    ViewData["PrintCloseUrl"] = Url.Action("Index", "Purchases");
}

<div class="container">
    <div class="header">
        <div class="header-right">
            <div class="company-name">شركة أبو آمن للأدوية</div>
            <div class="invoice-title-badge">فاتورة شراء</div>
            
            <div class="invoice-metadata" style="background:transparent; border:none; padding:0; margin-bottom:5px;">
                <div class="metadata-item">
                    <span class="label">رقم الفاتورة:</span>
                    <span class="value">#@Model.Id</span>
                </div>
                <div class="metadata-item">
                    <span class="label">التاريخ:</span>
                    <span class="value">@Model.PurchaseDate.ToString("yyyy-MM-dd")</span>
                </div>
            </div>
            
    <div class="invoice-metadata">
        <div class="metadata-item">
            <span class="label">المورد:</span>
            <span class="value">@Model.Supplier?.Name</span>
        </div>
        <div class="metadata-item">
            <span class="label">فاتورة المورد:</span>
            <span class="value">@(string.IsNullOrWhiteSpace(Model.InvoiceNo) ? "-" : Model.InvoiceNo)</span>
        </div>
        @if (!string.IsNullOrWhiteSpace(Model.Notes))
        {
            <div class="metadata-item">
                <span class="label">ملاحظات:</span>
                <span class="value">@Model.Notes</span>
            </div>
        }
    </div>
        </div>
        <div class="header-left d-flex align-items-center justify-content-center" style="width: 150px; height: 120px;">
            <img src="@Url.Content("~/img/abuamen-mark.svg")" style="width: 100%; height: 100%; object-fit: contain; display: block;" alt="شعار أبو آمن" />
        </div>
    </div>



    <table class="items-table">
        <thead>
            <tr>
                <th class="text-center" style="width: 50px;">#</th>
                <th>الصنف</th>
                <th style="width: 120px;">رقم الباتش</th>
                <th style="width: 140px;">تاريخ الانتهاء</th>
                <th class="text-center" style="width: 90px;">الكمية</th>
                <th class="text-center" style="width: 100px;">سعر الشراء</th>
                <th class="text-center" style="width: 120px;">الإجمالي</th>
            </tr>
        </thead>
        <tbody>
            @{
                int i = 1;
                foreach (var line in Model.Lines)
                {
                    <tr>
                        <td class="text-center">@(i++)</td>
                        <td>@line.Item?.NameAr</td>
                        <td class="text-center">@line.Batch?.BatchNo</td>
                        <td class="text-center">@((line.Batch != null) ? line.Batch.ExpiryDate.ToString("yyyy-MM-dd") : "-")</td>
                        <td class="text-center">@line.Qty.ToString("0.##")</td>
                        <td class="text-center">@line.UnitCost.ToString("N2")</td>
                        <td class="text-center">@line.LineTotal.ToString("N2")</td>
                    </tr>
                }
            }
        </tbody>
    </table>

    <div class="totals-section">
        <table class="totals-table">
            <tr>
                <td class="label">الإجمالي</td>
                <td class="value">@Model.SubTotal.ToString("N2")</td>
            </tr>
            @if (Model.Discount > 0)
            {
                <tr>
                    <td class="label">الخصم</td>
                    <td class="value">- @Model.Discount.ToString("N2")</td>
                </tr>
            }
            <tr class="grand-total">
                <td class="label">الصافي النهائي</td>
                <td class="value">@Model.NetTotal.ToString("N2")</td>
            </tr>
        </table>
    </div>

    <partial name="_InvoicePrintFooter" />
</div>
