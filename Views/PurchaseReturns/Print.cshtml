@model PurchaseReturn
@{
    Layout = "~/Views/Shared/_InvoicePrintLayout.cshtml";
    ViewData["PrintPageTitle"] = $"مرتجع مشتريات #{Model.Id}";
    ViewData["PrintCloseUrl"] = Url.Action("Index", "PurchaseReturns");
}

<div class="container">
    <div class="header d-flex justify-content-between align-items-start border-bottom mb-4 pb-3">
        <div class="header-right">
            <div class="company-name">شركة أبو آمن للأدوية</div>
            <div class="invoice-title-badge">مرتجع مشتريات</div>
            
            <div class="info-summary-grid">
                <div class="summary-item">
                    <span class="label">رقم المرتجع:</span>
                    <span class="value">#@Model.Id</span>
                </div>
                <div class="summary-item">
                    <span class="label">تاريخ المرتجع:</span>
                    <span class="value">@Model.ReturnDate.ToString("yyyy-MM-dd")</span>
                </div>
                <div class="summary-item main-info">
                    <span class="label">فاتورة الشراء:</span>
                    <span class="value">#@Model.PurchaseId</span>
                </div>
            </div>
        </div>
        <div class="header-left d-flex align-items-center justify-content-center" style="width: 150px; height: 120px;">
            <img src="@Url.Content("~/img/abuamen-mark.svg")" style="width: 100%; height: 100%; object-fit: contain; display: block;" alt="شعار أبو آمن" />
        </div>
    </div>

    <div class="info-grid">
        <div class="info-item">
            <strong>المورد</strong>
            @Model.Supplier?.Name
        </div>
        <div class="info-item">
            <strong>رقم فاتورة المورد</strong>
            @(string.IsNullOrWhiteSpace(Model.Purchase?.InvoiceNo) ? "-" : Model.Purchase!.InvoiceNo)
        </div>
        @if (!string.IsNullOrWhiteSpace(Model.Notes))
        {
            <div class="info-item" style="grid-column: span 2;">
                <strong>ملاحظات</strong>
                @Model.Notes
            </div>
        }
    </div>

    <table class="items-table">
        <thead>
            <tr>
                <th class="text-center" style="width: 50px;">#</th>
                <th>الصنف</th>
                <th style="width: 120px;">رقم الباتش</th>
                <th style="width: 140px;">تاريخ الانتهاء</th>
                <th class="text-center" style="width: 90px;">الكمية</th>
                <th class="text-center" style="width: 100px;">سعر الشراء</th>
                <th class="text-center" style="width: 120px;">الإجمالي</th>
            </tr>
        </thead>
        <tbody>
            @{
                int i = 1;
                foreach (var line in Model.Lines)
                {
                    <tr>
                        <td class="text-center">@(i++)</td>
                        <td>@line.Item?.NameAr</td>
                        <td class="text-center">@line.Batch?.BatchNo</td>
                        <td class="text-center">@((line.Batch != null) ? line.Batch.ExpiryDate.ToString("yyyy-MM-dd") : "-")</td>
                        <td class="text-center">@line.Qty.ToString("0.##")</td>
                        <td class="text-center">@line.UnitCost.ToString("N2")</td>
                        <td class="text-center">@line.LineTotal.ToString("N2")</td>
                    </tr>
                }
            }
        </tbody>
    </table>

    <div class="totals-section">
        <table class="totals-table">
            <tr class="grand-total">
                <td class="label">إجمالي المرتجع</td>
                <td class="value">@Model.Total.ToString("N2")</td>
            </tr>
        </table>
    </div>

    <partial name="_InvoicePrintFooter" />
</div>
